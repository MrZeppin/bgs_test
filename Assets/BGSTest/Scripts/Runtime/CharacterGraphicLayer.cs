namespace BGSTest
{

    // note: enum for quick implementation,
    // it could be changed to string or scriptable object so it's extendable without having to edit code
    public enum CharacterGraphicLayer
    {
        Base,
        Outfit,
        Hair,
        Hat,
    }
}